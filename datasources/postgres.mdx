---
title: "PostgreSQL"
description: "Add PostgreSQL as a data source for Quill"
mode: "wide"
---

#### 1. Create a read-only database user for Quill

```sql
CREATE USER quill_read_only WITH PASSWORD 'password';
```

#### 2. Enter database credentials in the Quill Portal

<img
  src="/images/postgres.svg"
  style={{
    boxShadow: "0px 1px 4px 0px rgb(63,82,149, 0.09)",
    marginLeft: 4,
    marginTop: 25,
    borderRadius: 16,
    borderWidth: 0.5,
    borderColor: "#E7E7E7",
  }}
/>

#### 3. Add a organization ID field and set an admin organization

<img
  src="/images/orgid.svg"
  style={{
    boxShadow: "0px 1px 4px 0px rgb(63,82,149, 0.09)",
    marginLeft: 4,
    marginTop: 25,
    borderRadius: 16,
    borderWidth: 0.5,
    borderColor: "#E7E7E7",
  }}
/>

#### 4. Create SQL views

<img
  src="/images/sqlview.svg"
  style={{
    boxShadow: "0px 1px 4px 0px rgb(63,82,149, 0.09)",
    marginLeft: 4,
    marginTop: 25,
    borderRadius: 16,
    borderWidth: 0.5,
    borderColor: "#E7E7E7",
  }}
/>
